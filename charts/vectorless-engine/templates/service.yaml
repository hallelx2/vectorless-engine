apiVersion: v1
kind: Service
metadata:
  name: {{ include "vectorless-engine.fullname" . }}
  labels:
    {{- include "vectorless-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vectorless-engine.selectorLabels" . | nindent 4 }}
